<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
    <%- include("./partials/nav.ejs") %>

    <main class="container">
      <article class="card">
        <!-- Blog Image -->
        <!-- <div class="card-image large <%= blog.image ? '' : 'no-img' %>"
             style="background-image:url(<%= blog.image ? '/uploads/' + blog.image : 'https://via.placeholder.com/800x400.png?text=No+Image' %>)">
        </div> -->
        <img
          src="<%= blog.image ? '/uploads/' + blog.image : 'https://via.placeholder.com/800x400.png?text=No+Image' %>"
          alt="<%= blog.title %>"
          style="
            width: 100%;
            max-height: 400px;
            object-fit: cover;
            border-radius: 10px;
          "
        />

        <!-- Blog Content -->
        <div class="card-content">
          <h2><%= blog.title %></h2>
          <p class="muted">
            <!-- By <%= blog.author %> | <%= blog.createdAt.toDateString() %> -->
            By Alex Milenye | <%= blog.createdAt.toDateString() %>
          </p>
          <p><%= blog.snippet %></p>
          <p><%= blog.body %></p>
        </div>
      </article>

      <a href="/" class="btn">← Back</a>
      <a href="/blogs" class="btn">Other Blogs</a>
    </main>

    <script>
      const trashcan = document.querySelector("a.delete");

      trashcan.addEventListener("click", (e) => {
        e.preventDefault(); // stop normal link navigation

        const endpoint = `/blogs/${trashcan.dataset.doc}`;

        fetch(endpoint, { method: "DELETE" })
          .then((response) => response.json())
          .then((data) => (window.location.href = data.redirect))
          .catch((err) => console.log(err));
      });

      // images
      document.querySelectorAll(".card-image").forEach((div) => {
        const bg = div.getAttribute("data-bg");
        if (bg) div.style.backgroundImage = `url(${bg})`;
      });

      // menu toggle

      const hamburger = document.getElementById("hamburger");
      const navMenu = document.getElementById("nav-menu");

      hamburger.addEventListener("click", () => {
        navMenu.classList.toggle("show");
      });
    </script>
  </body>
</html>
